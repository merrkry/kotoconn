# Benchmarks

After [workspace setup](../docs/build.md), run `mise exec -- moon run benchmark:run`.
Read the [TUN measurement guide](tun/README.md) before comparing results or changing
workloads. Use `benchmark:test-smoke` to check the pipeline without collecting a
performance baseline.
